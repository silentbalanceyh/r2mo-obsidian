---
runAt: 2026-04-03.15-53-08
title: 泳道图BUG
---
- 只有点击了上边的按钮泳道图才能显示出来，并且泳道图这个页签应该可以支持关闭
- 泳道图的背景色是黑色，而我目前用的是浅色模式
- 泳道图不能按照项目划分卡片，应该是左侧有项目名名称，然后历史任务数量，右侧的方块只显示目前在跑任务的方块
- 项目展开后才显示子项目信息，且没有任何需求说了每个项目两行，一行历史一行在运行

## Changes
- 2026-04-03: [Agent] 修复5个BUG：(1)泳道图页签不再预创建，由按钮打开，已存在则切换过去；(2)mainTabWidget启用tabsClosable，HOME页签不可关闭，泳道图可关闭；(3)container和swimlaneContent显式设置background:white修复黑色背景；(4)重写泳道图布局——左侧项目名+历史任务数(Hist.列)，右侧只显示正在跑的任务队列方块(橙色)，不再按卡片划分；(5)每个项目一行，子项目用└缩进显示在父项目下方
- 2026-04-03: [Agent] 修复历史任务统计错误：getHistoricalTasks原来扫描task/下的*.md文件（与队列文件重叠），改为扫描task/YYYY-MM-DD/子目录中的md文件；Hist.列和历史数改为左对齐
- 2026-04-03: [Agent] 修复getTaskQueueCount过滤器从*.md改为task-*.md避免统计非队列文件；添加/删除按钮改为segmented control布局(addBtnLeft/removeBtnRight)与中|En一致；泳道图去掉左侧空白——项目名自适应宽度、方块紧跟其后、空位不再渲染、grid末列stretch右推空白
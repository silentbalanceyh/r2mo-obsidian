---
runAt: 2026-04-03.16-22-34
title: 泳道图BUG
---
1. 左侧项目名称的宽度要上下对齐（比如用一个固定宽度），这样用到会看起来从同一个地方出发
2. 空的泳道用灰色边框
3. 在执行的任务用蓝色，不用橙色
4. 历史数量依旧统计不对，不可以用 task- 的方式统计，要统计带有 yaml 的 title 的
5. 历史数量位置可以双击，双击打开一个新的弹窗显示历史数据（按日期分组，逆序）

## Changes
- 2026-04-03: [Agent] 修复泳道图对齐与交互：项目名称列改为固定宽度并左对齐；空泳道显示灰色边框；运行中任务颜色改为蓝色；历史统计改为仅统计带 YAML frontmatter title 的历史任务文件（并据此更新队列读取）；历史数量标签支持双击，弹出按日期分组且逆序的历史任务窗口。
- 2026-04-07: [Agent] 修复历史任务路径错误：`.r2mo/history/YYYY-MM-DD/` 改为 `.r2mo/task/history/YYYY-MM-DD/`。
- 2026-04-07: [Agent] 子项目扫描仅识别 DPA 架构（-ui, -provider, -domain, -api 后缀），其他目录如 web 不作为子项目显示。
- 2026-04-07: [Agent] 历史数量点击改为单击触发弹框（从 `MouseButtonDblClick` 改为 `MouseButtonPress`）。
- 2026-04-07: [Agent] 历史数为0时不显示数字，只显示空白；vault header（每张卡片项目标题）使用 Primary 色（#007aff）背景横向高亮整行，文字改为白色。
- 2026-04-07: [Agent] 每张项目卡片之间添加 12px 间距。

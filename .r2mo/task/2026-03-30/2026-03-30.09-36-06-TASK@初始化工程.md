---
runAt: 2026-03-30.08-48-52
title: 初始化工程
---
技术栈，我将打造一个 obsidian 的管理中心，可以同时管理多个 Vault 中的信息，并且针对特定文件进行设置后呈现在右侧工作区。
- 高速切换，多个 Agent 会管理
- 技术栈：Qt C++（检索操作系统中Qt的版本，初始化相对应的工程）
- 先初始化一个基本工作区（带菜单），方便后续扩展
- 每个 Vault 可以使用 obsidian 打开（可设置 app 路径），工作台中不直接编辑 md，只查看
- 记住书写 .gitignore（.r2mo保留）
在项目中创建
- CLAUDE.md
- AGENTs.md
提取开发规范和相关的 rules，并且写成 Agent 接下来可处理的基础引导规则

## Changes
- 2026-03-30: [Orchestrator] Completed task - 初始化 Qt 6.10.0 C++ 工程
  - 检测 Qt 版本: 6.10.0 (路径: ~/Qt/6.10.0/macos)
  - 创建 CMakeLists.txt (CMake 3.16+, C++17, Qt6::Core + Qt6::Widgets)
  - 创建 src/main.cpp (应用入口)
  - 创建 src/mainwindow.h/cpp (主窗口，含菜单栏、Vault列表、预览面板)
  - 功能: 添加/删除 Vault、打开 Obsidian、设置 App 路径、预览 Vault 信息
  - 创建 .gitignore (保留 .r2mo 目录)
  - 创建 CLAUDE.md (Qt 开发规范、构建指南、项目结构)
  - 创建 AGENTS.md (Agent 任务处理规则、代码生成规范)
  - 创建 dev-start.sh / dev-stop.sh / dev-build.sh (开发脚本)
  - 构建验证: cmake -B build + cmake --build build 成功生成 ObsidianManager.app
---
runAt: 2026-03-30.08-48-54
title: 项目详情页BUG
---
- 概览页签中红色文字不用加粗，背景色去掉
- 表格 Vault Statistics 和红色文字中间应该有间距，和下边间距一样，8px 左右
- 两个表格：Vault 和 R2MO 宽度应该铺满整个页面，并且第一列和第二列宽度应该相同
- 项目任务中的树型结构，展开时左侧的图标应该发生变化，否则无法从视觉上看出来是树
- 结构图：背景应该是 Grid（去掉标尺），Grid 应该横向和纵向都铺满全页

## Changes
- 2026-03-30: [Orchestrator] Fixed all bugs:
  - 概览页签：红色文字不加粗（font-weight: 400），去掉背景色
  - 表格间距：红色文字和表格之间有 8px 间距（margin: 0 0 8px 0）
  - 表格宽度：铺满整页（width: 100%, table-layout: fixed），两列等宽（50%/50%）
  - QTextEdit：去掉边框和边距（setFrameShape(NoFrame), setDocumentMargin(0)）
  - 树型结构：展开/收起图标切换（▶/▼），连接 itemExpanded/itemCollapsed 信号
  - 结构图：去掉标尺，使用 Grid 背景（横向和纵向都铺满全页），去掉 fitInView
  - 更新文件：mainwindow.h, mainwindow.cpp
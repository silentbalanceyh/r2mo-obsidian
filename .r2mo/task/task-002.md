---
runAt: 2026-04-22.08-42-57
title: 远程项目选择器BUG
author:
---
远程项目选择器要可以选择 /media/ 下的路径，这个是外挂路径，其次左侧的三角符号的样式要和本地选择器一样，支持树型展开行为，注意远程的树型展开要可以延迟加载，包括行为，目前左侧的三角符号看不见，而仓库名称有了之后就不用提示再输入项目名称了。

## Changes
- 2026-04-22: [Team Leader] 完成 task-002，补齐远程目录选择器的 /media 根节点入口、无权限时仅展示不可展开根节点、对齐本地选择器的三角图标与延迟加载行为，并让远程仓库名称在留空时自动按所选路径推导。
- 2026-04-22: [Team Leader] 修复远程实时监控误判，移除无进程时仅凭 `.omc` 伪造 OpenCode 行的回退逻辑，并改为从 Claude 会话文件内容中的 `cwd` 反查项目归属，解决嵌套远程仓库会话 ID 丢失与状态不准问题。
- 2026-04-22: [Team Leader] 调整默认窗口尺寸为 1680x1050，并将新增高度优先分配给实时监控板上半区，保持下方计费板默认高度基本不变。
- 2026-04-22: [Team Leader] 将实时监控板上半区自动刷新频率从 10 秒下调到 1 分钟，降低引入 SSH 远程扫描后的轮询压力，并补充刷新频率守护测试。
- 2026-04-22: [Team Leader] 将监控板刷新改为本地/远程并行、远程仓库并行和计费 source 并行抓取，同时把上下两块的刷新反馈改为行内 `Refreshing...`，避免整表遮罩阻塞查看。
- 2026-04-22: [Team Leader] 追加监控板按项目分批回填，首批项目完成即刷新对应行，并修正 OpenCode 在缺少活跃 artifact/global 证据时仍被 CPU 噪声误判为 Working 的问题。
- 2026-04-22: [Team Leader] 收口监控板用户可见状态，移除 `Unknown` 文案与专属灰色渲染路径，统一将非 Working 的占位/详情显示归并为 `Ready`，并补充守护测试防止回归。
- 2026-04-22: [Team Leader] 恢复实时监控板项目边界，只按左侧项目列表已登记的本地/远程仓库生成监控行，不再额外展开 R2MO 子项目，并新增守护测试锁定该行为。

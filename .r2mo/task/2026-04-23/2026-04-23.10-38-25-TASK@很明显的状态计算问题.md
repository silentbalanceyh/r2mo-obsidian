---
runAt: 2026-04-23.06-44-03
title: 很明显的状态计算问题
author:
---
目前我的机器上虽然开了很多 Terminal，但是并没有 SSH 连接到远程去，这种场景下为何我的实时监控板中会有配置的远程项目的 OpenCode 的会话呢？而且远程上去的就绪时间是6小时或7小时，我觉得基于远程的数据捕捉和抓取是否需要精细或细粒度？索性移除右边的时间不做相关计算如何？

- 远程会话必须建立在真实 SSH 远程工具进程之上，不能只根据历史 artifact 回填伪会话
- 状态列不再展示累计运行时长，避免 `Ready` 被旧 artifact 时长误导
- 远程仓库选择时不用提示再输入项目名称

## Changes
- 2026-04-23: [Team Leader] 完成 task-002：收紧 `RemoteSessionScanner` 的远程 OpenCode 回填逻辑，只有 SSH probe 已发现真实远程工具进程时才允许使用 artifact 补全会话，避免未连接远程时仍显示配置远程项目的 OpenCode 会话；同时调整监控板状态列为纯 `Working/Ready` 标签，不再拼接运行时长，消除 6 小时或 7 小时这类误导性 Ready 显示；补充 `tests/task_002_remote_opencode_artifact_fallback_test.sh` 与 `tests/task_002_monitor_runtime_display_guard_test.sh` 守卫，并同步更新 task-002 验证套件。

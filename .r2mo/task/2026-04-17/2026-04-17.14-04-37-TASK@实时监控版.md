---
runAt: 2026-04-03.15-53-10
title: 实时监控版
---
创建一个新按钮（泳道图旁边）
- 泳道图使用持久化的SVG，不要使用emoji，HOME也一样
- 新按钮为：监控板
监控板中的核心落地功能
- 所有项目列表一旦添加进来后，可以在一个表格中试试查看当前正在使用的AI工具
- AI工具打开了之后监控正在运行的会话 Session，不论是 WezTerm、还是 iTerm 都要可以捕捉
- 针对 Session 的状态进行监控
	- 绿色：正在工作
	- 蓝色：就绪等待
这个监控板要保证多个任务可以直接查看详情

## Changes
- 2026-04-17: [AI Agent] Completed task - Implemented Monitor Board feature with full session monitoring
  - Created `SessionScanner` utility class (`src/utils/sessionscanner.h/.cpp`) for detecting AI sessions from WezTerm, iTerm2, Terminal.app via process tree scanning and AI tool session file scanning
  - Added Monitor Board button in toolbar with SVG icon (next to Swimlane button)
  - Implemented monitor tab with real-time session status table: green=Working, blue=Ready
  - Session detection covers both terminal processes (WezTerm, iTerm2, Terminal) and file-based sessions (Claude, Codex, OpenCode, Gemini, Cursor, Trae)
  - Added session detail dialog on double-click for viewing full session information
  - Auto-refresh every 15 seconds with async scanning
  - Replaced all emoji icons (Swimlane, HOME, stats headers) with persistent SVG icons
  - Added `ui-icons.qrc` resource file with SVG icons for swimlane, monitor, and home buttons
  - Build verified successfully